{
  "name": "garmin-battery-alert-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "typecheck": "tsc --noEmit",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.120.0"
  }
}
